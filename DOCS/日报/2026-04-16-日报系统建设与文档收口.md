# 2026 年 4 月 16 日 — 日报系统建设与文档收口

## 今日目标

整理昨天的 B5-2 解决记录归档入库，同时补全文档链条。

## 过程记录

### 阶段一：发现日报里缺少 BOOT-012 的实现细节

昨天写的 B5-2 日报只写了"在 hyp 侧判断 `[hpa, hpa + size)` 是否落在合法 BAR 范围内"这个结论，没有写具体怎么实现的。

重新看了内核 commit `501ca0cb907f` 的完整 diff，发现实际做了 4 件事：

1. **扩展 manifest**：在 `struct pkvm_boot_ptdev_manifest_entry` 里新增 `bars[]` 数组，每个 BAR 记录 `base` 和 `size`，启动期 `build_boot_ptdev_manifest()` 遍历所有 PCI 设备把 memory BAR 全部存进去
2. **提供命中判断 helper**：`pkvm_boot_ptdev_bar_contains()` 公式是 `hpa >= bar_base && hpa + size <= bar_base + bar_size`，封装成两个对外函数分别面向"全量 manifest 查"和"当前 VM attached 设备查"
3. **`pkvm_vm_mmu_map()` 三段分流**：命中 BAR → 直接 MMIO leaf；命中 host RAM → 走 donate；两者都不是 → 直接拒绝
4. **teardown 跳过 undonate**：BAR leaf 只拆 Guest EPT，不调 `__pkvm_host_undonate_guest()`

补完这些细节后，amend 到昨天 commit 里。

### 阶段二：日报归档与模版提炼

把 `DOCS/tmp/B5-2-问题解决记录.md` 移到 `DOCS/日报/2026-04-15-B5-2问题解决记录.md`，同时提炼了日报书写模版到 `DOCS/日报/模版.md`。

模版核心要点：
- 迭代链条不能断：每解决一个问题暴露下一个，继续在同一篇里写
- 调用链用 text 代码块 + 缩进
- 关键日志原样贴
- 每阶段统一：现象 → 根因 → 解决 → 结果
- 所有引用带路径

### 阶段三：废弃 todo 文件清理

发现 `DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/TODOs/todo.md` 已无维护价值（所有条目都已落地或废弃），随日报 commit 一并删除。

## 今日未完成

无。

## 关联 commit

| commit | 作用 |
|--------|------|
| `83885f7` | 补全 B5-2 日报：HPA 校验为核心，迭代过程顺带修复的 bug 为障碍 |
| `020d50d` | 固化 T4A 三类样例十轮矩阵结论 |
| `3bde062` | 补充 run-crosvm 交互式使用说明 |
| `2ac763d` | 同步交互式使用说明到索引入口 |

## 关联本地文档

- 日报记录：`DOCS/日报/2026-04-15-B5-2问题解决记录.md`
- 日报模版：`DOCS/日报/模版.md`
- B5-2 实现跟踪：`DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/实施跟踪/01E-T10-B5-2-protected-pVM-Guest-EPT建图边界实现.md`

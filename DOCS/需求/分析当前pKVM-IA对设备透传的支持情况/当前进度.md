目标: 将 pVM 设备透传从“源码设计可行”推进到“可分阶段实施、可持续跟踪、可验证收敛”。

说明:
- 本文件保留专题级摘要与入口。
- 任务状态、阶段结论和验证闭环以 `实施跟踪/00-总览与进展看板.md` 和 `实施跟踪/07-验证矩阵.md` 为准。

当前进度:
- 已确认推荐主线是方案 B: `donate + pVM DMA mirror pgtable`。
- 已确认当前代码里已经存在 `pgstate_pgt -> ptdev->pgt -> IOMMU SLPTR` 这条骨架，但缺少预填充、运行时 mirror、teardown 收敛等关键补全。
- 已建立 `实施跟踪/` 目录，后续实施拆分、依赖关系、状态推进和验证矩阵统一在该目录维护。
- 已补一份 `run-crosvm.sh` 交互式使用说明，固定“两终端工作法”、guest 登录方式和 T4A `Case A/B/C` 的最小手工执行套路。
- 已完成 T1 修复并通过当前首轮运行验证：旧 shadow spgt 残留 refcount 导致的 donate 阻塞不再复现。
- 已补齐 GitHub Issue / PR 模板，并把 `pKVM-IA` 纳入当前 superproject 的 submodule 管理，后续将按 `Issue -> pKVM-IA PR -> pkvm-x86 PR` 的闭环推进。
- superproject 当前已同步到 `pKVM-IA` 的 `1cfdacc20432`，对应 `pKVM-IA` PR `#3`（T4：在 VM teardown 前预先封锁 `ptdev` DMA）的合并点。
- 已在 GitHub 初始化远端管理对象：`pkvm-x86` Project `#3`；截至当前，远端已维护 `19` 个 Epic / Task / Bug issues。
- 已新增真正的主线前置任务：`pkvm-x86#13`，用于收敛 protected pVM guest/hyp 的 passthrough MMIO contract。
- 已新增一套可复用的 host/guest tracing helper，用于把“NVMe BAR MMIO 是否 direct”和“NVMe I/O 是否经 DMA API 提交”拆成固定证据：
  - 入口：`DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/实施跟踪/验证记录/t11-p1-nvme-mmio-dma-trace-helper/README.md`
  - host 侧负责抓 `SET_PTDEV_MMIO_METADATA`、`sync_ptdev_mmio_metadata` 与 `kvm_sev_es_mmio_*`
  - guest 侧负责抓 `nvme_map_data`、`dma_map_bvec` / `dma_map_sgtable`、`nvme_write_sq_db`、`pkvm_direct_mmio_*` 与 `mmio_*`
- 已完成 `T11-P2` 首轮实测（2026-04-16，`0000:01:00.0`）：
  - protected pVM 内对 `/dev/nvme0n1` 执行 `dd if=/dev/nvme0n1 of=/dev/null bs=1048576 count=8 iflag=direct`
  - guest `function_graph` 已抓到 `dma_map_sgtable() -> __dma_map_sg_attrs() -> dma_direct_map_sg() -> swiotlb_map()`，说明这轮数据面确实走了 DMA API / DMA 映射路径
  - guest 同时抓到 `pkvm_mmio_writew()/pkvm_mmio_writel() -> pkvm_virt_mmio()`，而 host `kvm_sev_es_mmio_read/write` 计数为 `0`
  - 结合 `pkvm_virt_mmio()` 的 direct / fallback 分流源码，本轮可以高置信推断：这次 NVMe BAR MMIO 访问落在 direct 路径，而不是 host fallback
  - 记录入口：`DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/实施跟踪/验证记录/t11-p2-protected-vfio-nvme-mmio-dma-0100-20260416.md`
- 已新增 `pkvm-x86#20`，用于单独收敛“启动阶段可信、运行期 Host 不可信”前提下的两个问题：
  - 问题 1：不在 Host 启动时扫描到的设备名单里的设备，一律不允许透传给 pVM
  - 问题 2：protected pVM Guest EPT `GPA -> HPA` 建图是否仍可被运行期 Host 影响或篡改
- 已新增 `pkvm-x86#21`，用于承接问题 1 的实现阶段；当前语义只覆盖启动期 `platform manifest` 冻结与 checked `ptdev` 创建收口。
- 已确认 T1 对应的内核提交已经直接位于 `pKVM-IA` 默认分支，因此无法再补一条对默认分支有实际 diff 的正常 PR；后续内核改动将从 topic branch + PR 流程开始。
- `pkvm-x86#2` 对应的 T1 任务已经完成并通过首轮验证，当前可以关闭；后续阻塞已前移到 `pkvm-x86#5` 和 `pkvm-x86#13`。
- `pkvm-x86#3` 对应的 BOOT-006 bug 也已满足关闭条件：旧 donate/refcount 失败签名已稳定不再复现，当前阻塞同样已前移到 `pkvm-x86#5` 和 `pkvm-x86#13`。
- `pkvm-x86#21` 对应的问题 1 已完成；后续 B5 主线当前前移到 `pkvm-x86#20` 的问题 2：Guest EPT `GPA -> HPA` 建图边界，而不是旧的泛化设备真实性表述。
- 已新增 `B5-2` 第一轮源码梳理文档，用于单独固定 Guest EPT 建图主链、Host 可控输入、`check_donation()` 当前约束，以及 Guest EPT 主建图与 `pgstate_pgt` DMA mirror 的边界。
- 已完成 `B5-2` 第二轮入口分类梳理：
  - 当前找到的 Host 运行期主建图入口仍只有 `page fault -> vm_mmu_map -> __pkvm_host_donate_guest`
  - `vm_mmu_unmap` / `vm_mmu_age` 对 protected VM 已被拒绝
  - `pkvm_vm_mmu_destroy()` 中的 `__pkvm_host_undonate_guest()` 属于 teardown 回收，而不是运行期 Host 任意改图
  - `PKVM_GHC_SHARE_MEM` / `PKVM_GHC_UNSHARE_MEM` 会改 Guest EPT page-state，但复用的是当前 Guest EPT 已解析出的 HPA
  - `SET_PTDEV_MMIO_METADATA`、MMIO allowlist 和 `pgstate_pgt` 只影响 DMA mirror / 元数据，不直接改主 Guest EPT
- 因此问题 2 当前剩余缺口已进一步收窄为：Host 在首次 page-fault 建图时给出的 candidate HPA，是否已经被现有 hyp 约束充分证明“就是这页”。
- 已按源码修正 `B5-2` 第三轮 candidate HPA 分析：
  - 前一版把 `KVM_X86_SW_PROTECTED_VM` 的 generic private-memory 语义误套到了真正的 pKVM protected VM；当前本树真正的 pVM 是 `KVM_X86_PKVM_PROTECTED_VM`
  - 对真正的 pKVM protected pVM，`has_private_mem` 不打开，`PFERR_PRIVATE_ACCESS` 也不注入，因此当前 Guest EPT 建图不走 `guest_memfd private/shared` 分叉
  - 当前 `fault->pfn` 仍走 `__gfn_to_pfn_memslot()` -> `__gfn_to_hva_many()` -> `hva_to_pfn()`
  - 透传 MMIO 的访问处理路径也已纠偏：guest 先走 `pkvm_virt_mmio()`；命中 allowlist 的 `DIRECT_BAR` 时 direct `raw_read*/raw_write*`，未命中才退回 `PKVM_GHC_IOREAD/IOWRITE`
  - 但 direct BAR 的首次 Guest EPT 建图仍回到 `page fault -> vm_mmu_map` 主链；若 memslot backing 是 remapped MMIO，则 `hva_to_pfn()` 会通过 `VM_IO | VM_PFNMAP` 分支解析 PFN
- 因此问题 2 当前更准确的剩余问题已改写为：B5 关心的 Guest EPT leaf 中，哪些是普通 RAM memslot，哪些是 remapped BAR memslot；在 `vm_mmu_map -> __pkvm_host_donate_guest` 主链上，hyp 当前是否已经充分证明 Host 给出的 candidate HPA 语义正确。
- 已新增 `B5-2` 第四轮语义约束梳理：
  - 对真正的 pKVM protected pVM，candidate HPA 在进入 hyp 前就已经由 Host/KVM-high 的 memslot/HVA 路径解析完成；hyp 收到的是已定好的 `hpa`，而不是待重新证明语义的 memslot 请求
  - 当前 `check_donation()` / `host_initiate_donation()` 主要检查 ownership、`PKVM_NOPAGE`、`mem_range` 与 refcount，不独立证明“这个 GPA 就该绑定这块 HPA”
  - `struct pkvm_vm` / `struct pkvm_shadow_vm` 当前也没有独立于运行期 Host 的 per-GPA truth source；前者只有 guest MMU，后者只有 DMA mirror / MMIO allowlist
  - 因此在“启动阶段可信、运行期 Host 不可信”的模型下，普通 RAM leaf 的首次建图当前确实仍存在 candidate HPA 语义约束缺口；这应表述为“首次建图完整性问题”，而不是“已有 leaf 重绑问题”
  - `pvmfw` GPA 区间与 direct BAR / `VM_IO | VM_PFNMAP` leaf 需要继续单独看待，不应直接和普通 RAM leaf 混成同一个结论
- 已补一条 `B5-2` 边界澄清：
  - 当前 `B5-2` 已落地的是“Guest EPT 建图边界校验”，不是“Host 被显式撤销了对 assigned BAR 的 CPU 访问权”
  - 目前只看到 IOMMU MMIO 有 `pkvm_host_ept_unmap()` 式 Host CPU 收口；普通 assigned BAR 路径尚未看到同类 revoke 动作
  - 因此这条缺口需要后续作为独立问题继续处理，但先不与当前 `T4` teardown DMA 生命周期主线混做
- 已把“Host 对 assigned BAR 的 CPU 访问权收口”单独拆成 `pkvm-x86#34`：
  - 当前作为 `B5-2` 的 follow-up Task 跟踪，不再混写在 Guest EPT 建图边界已完成结论里
  - 当前状态先标为 `blocked`：依赖 `T4` 的 DMA 生命周期收敛，以及 `T6` 的 remove-path / rollback 语义补齐后，再决定 BAR revoke / restore 的最终挂点

当前卡点:
- `BOOT-007` 的旧签名已经在 2026-03-27 的有效重测中消失，当前主线状态已经正式切到 `BOOT-008/T2/T3`。
- `BOOT-008` 的 host DMAR 主签名在最新带 patch 的实机验证中已不再复现：
  - host `dmesg` 未再出现 `DMA Read NO_PASID` / `PTE Read access is not set`
  - guest 已启动到 Ubuntu login prompt，并完成 NVMe 原始读与 `mkfs.ext4 -F /dev/nvme0n1`
- `BOOT-009` 的强关联偶发签名也已完成单独修复并可视为已关闭：
  - 修复入口已收敛到 [memory.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/memory.c) 的 protected guest GPA 回写语义
  - 对应内核提交：`b86cfd0230b9`
  - 对应问题单： [BOOT-009-protected-pVM-NoIommu-VFIO-copy-gpa-exception14-soft-lockup.md](/home/mrgeek/pkvm-x86/DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/问题记录/BOOT-009/BOOT-009-protected-pVM-NoIommu-VFIO-copy-gpa-exception14-soft-lockup.md)
  - 对应实施任务： [01D-B4-protected-pVM-allowlist-guest-gpa回写路径修复.md](/home/mrgeek/pkvm-x86/DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/实施跟踪/01D-B4-protected-pVM-allowlist-guest-gpa回写路径修复.md)
- 当前剩余卡点已经不再是已知启动/早期运行签名，而是后续补全项：
  - teardown 生命周期还没有独立完成验收
  - 首次 attach / prepopulate 还没有单独收敛
  - VFIO remove-path 与失败回滚还没有进入实现
  - 若要把数据路径作为硬证据，还需要一轮显式 mount 后的文件级写入/回读
- 当前 host->pKVM 的 BAR/MMIO metadata contract、runtime DMA mirror patch 和 `B4` guest copy 语义修正都已落地，因此主线已从“启动 correctness blocker”前移到“生命周期与回归验证缺口”。
- 已确认 ARM64 pKVM/AVF 可以作为重要参考对象，但参考重点应放在：
  - device metadata / manifest 通道
  - guest MMIO 分类机制
  - pKVM/hyp 持有 authoritative device state
  - 而不是直接照搬 ARM 的设备 assignment 代码路径
- 已新增 ARM 参考源清单，并约定后续本地参考代码统一落在未跟踪的 `refs/` 目录：
  - `DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/参考实现/ARM-pKVM-参考源.md`
- 已基于本地 ARM 参考树整理出一版设计映射：
  - `DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/参考实现/ARM-pKVM-到-x86-设计映射表.md`
- B3 已收敛出一个更合适的推进方式：先做“上层方案收敛”，再继续细化底层 contract。
- 当前倾向的低复杂度第一阶段目标是：
  - protected pVM
  - 单个 VFIO PCI 设备
  - 静态 attach
  - `NoIommu`
  - config space 继续 emulated
  - 普通 BAR MMIO 作为优先打通的访问面
  - MSI-X table/PBA 继续 emulate 或列为第一阶段非目标
- 在这个上层方案稳定后，B3 再继续细化：
  - 以“受信的 GPA 直达区间表”作为 guest/hyp contract
  - userspace -> host KVM：复用 `KVM_CAP_X86_PROTECTED_VM` 的 `KVM_ENABLE_CAP` flag 空间提交候选区间表
  - authoritative allowlist：必须最终存放在 pKVM/hyp，而不是停留在 host KVM
  - guest -> pKVM/hyp：使用“共享缓冲区 + 分块 hypercall 拷贝”查询直达区间表
  - 注册项至少携带 `guest_gpa + bdf + pasid + bar_index + bar_offset + size + flags`
- B3-1 上层方案已单独沉淀到：
  - `DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/实施跟踪/01C-1-B3-1-protected-pVM-设备透传第一阶段上层方案.md`
- B3-2 已开始细化第一阶段所需的 x86 `ptdev metadata` 结构，当前方向是：
  - `ptdev` 继续承载 identity / IOMMU / attachment state
  - 新增 hyp 内部的富 MMIO metadata
  - guest 只获取精简 allowlist
  - 文档：`DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/实施跟踪/01C-2-B3-2-x86-ptdev-metadata-最小结构草案.md`
  - 当前接口方向：
    - userspace -> host KVM：继续沿用 `KVM_ENABLE_CAP(KVM_CAP_X86_PROTECTED_VM)`
    - guest -> pKVM/hyp：新增查询 allowlist 的 guest hypercall
  - 当前 ABI 决议：
    - metadata 必须在第一次 `KVM_RUN` 前提交
    - 第一阶段冻结 metadata，不支持运行期修改
    - guest 启动早期查询并缓存 allowlist
    - guest allowlist 只暴露 `DIRECT_BAR`
  - 当前已进一步收敛：
    - 推荐 UAPI 常量名与结构体命名
    - 提交接口与查询接口的错误码
    - 幂等提交语义
  - 当前第一轮实现已启动，并已完成：
    - [asm/kvm.h](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/include/uapi/asm/kvm.h) 的 `SET_PTDEV_MMIO_METADATA` UAPI 常量与 userspace 提交结构
    - [kvm_host.h](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/include/asm/kvm_host.h) 的 host 侧 metadata 缓存
    - [pkvm_host.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/vmx/pkvm/pkvm_host.c) 的第一版 copy/validate/cache 处理
    - [pkvm_hypercalls.h](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/include/asm/pkvm_hypercalls.h)、[hyp/vmexit.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/vmexit.c)、[hyp/pkvm.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/pkvm.c) 的 host -> hyp metadata 同步通路
    - [hyp/ptdev.h](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/ptdev.h) 和 [hyp/ptdev.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/ptdev.c) 的 hyp 侧 metadata 绑定
    - [linux/kvm_para.h](/home/mrgeek/pkvm-x86/pKVM-IA/include/uapi/linux/kvm_para.h) 的 guest allowlist 查询 ABI
    - [pkvm/vmx/vmx.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/pkvm/vmx/vmx.c) 的 guest `INFO/READ` hypercall 处理
    - [arch/x86/coco/pkvm/pkvm.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/coco/pkvm/pkvm.c) 的 allowlist 缓存与 `pkvm_virt_mmio()` 分流
    - crosvm 的第一版 metadata 提交通路：
      - [hypervisor/src/x86_64.rs](/home/mrgeek/pkvm-x86/crosvm/hypervisor/src/x86_64.rs) 的 protected pVM ptdev metadata 抽象
      - [hypervisor/src/kvm/x86_64.rs](/home/mrgeek/pkvm-x86/crosvm/hypervisor/src/kvm/x86_64.rs) 的 `KVM_ENABLE_CAP(KVM_CAP_X86_PROTECTED_VM, SET_PTDEV_MMIO_METADATA, ...)` 提交
      - [devices/src/pci/vfio_pci.rs](/home/mrgeek/pkvm-x86/crosvm/devices/src/pci/vfio_pci.rs) 的 VFIO BAR sparse-mmap -> ptdev metadata 导出
      - [arch/src/lib.rs](/home/mrgeek/pkvm-x86/crosvm/arch/src/lib.rs) 的 PCI 设备注册阶段 metadata 提交 hook
      - [src/crosvm/sys/linux/device_helpers.rs](/home/mrgeek/pkvm-x86/crosvm/src/crosvm/sys/linux/device_helpers.rs) 和 [src/crosvm/sys/linux.rs](/home/mrgeek/pkvm-x86/crosvm/src/crosvm/sys/linux.rs) 的 protected VM 调用路径
  - 已完成 crosvm userspace 本地构建验证：
    - 已安装 Rust `1.77.2` 工具链并完成 `cargo build -p crosvm --locked`
    - 构建过程中已修复的本地编译问题包括：
      - [hypervisor/src/kvm/x86_64.rs](/home/mrgeek/pkvm-x86/crosvm/hypervisor/src/kvm/x86_64.rs) 的常量名误写
      - [arch/src/lib.rs](/home/mrgeek/pkvm-x86/crosvm/arch/src/lib.rs) 的 `DeviceRegistrationError` 枚举排序约束
      - [x86_64/src/lib.rs](/home/mrgeek/pkvm-x86/crosvm/x86_64/src/lib.rs) 对 `components.hv_cfg.protection_type` 的字段访问修正
  - 已完成第二轮 crosvm userspace 路径收敛并通过本地构建：
    - [hypervisor/src/kvm/x86_64.rs](/home/mrgeek/pkvm-x86/crosvm/hypervisor/src/kvm/x86_64.rs) 的 `KvmVm::is_pkvm()` 已改为复用 `get_protected_vm_info()`
    - [x86_64/src/lib.rs](/home/mrgeek/pkvm-x86/crosvm/x86_64/src/lib.rs) 已在 protected VM 下停止：
      - 创建 `PciVirtualConfigMmio`
      - 向 `generate_pci_root()` 传入 `vcfg_base`
      - 在 ACPI 中公开 `VCFG`
    - 已本地复用绝对路径验证通过：`/home/mrgeek/.cargo/bin/cargo build -p crosvm --locked`
  - 已完成 2026-03-27 的有效端到端运行验证：
    - guest 已启动到 Ubuntu login prompt
    - `Failed to map mmio page; failed to create vm mapping` 未再出现
    - `vcpu hit unknown error: Bad address (os error 14)` 未再出现
    - host dmesg 新暴露出的独立签名为：
      - `DMAR: [DMA Read NO_PASID] Request device [01:00.0] fault addr ... [fault reason 0x06] PTE Read access is not set`
    - 当前新问题已单独记录为：
      - [BOOT-008-protected-pVM-NoIommu-VFIO-host-DMAR-PTE-Read-access-not-set.md](/home/mrgeek/pkvm-x86/DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/问题记录/BOOT-008/BOOT-008-protected-pVM-NoIommu-VFIO-host-DMAR-PTE-Read-access-not-set.md)
  - 已修复当前 host 内核编译 blocker：
    - [pkvm/vmx/vmx.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/pkvm/vmx/vmx.c) 的 `__PKVM_HYP__` guest allowlist 回写路径误用了普通 KVM 的 `kvm_vcpu_write_guest()`
    - `vmlinux` 链接阶段因此出现 `undefined reference to kvm_vcpu_write_guest__pkvm`
    - 现已改为使用 hyp 侧 [memory.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/memory.c) 的 `write_gpa()`
    - 已本地增量验证通过：`make -C pKVM-IA -j1 vmlinux`
- B2 当前已完成其 workaround 目标：旧失败点已明显后移；它不替代后续 DMA mirror 主线。
- 当前已经需要把新签名单独拆出：
  - `BOOT-007` 的旧签名已不再复现
  - 新的 host DMAR fault 已单独记录为 `BOOT-008`
- `pgstate_pgt` 当前还带有“pVM teardown 时 undonate 页面”的旧语义，需要收敛成纯 DMA mirror 语义。
- VM 销毁阶段当前是“先销毁页表，再处理 ptdev”，且现有 `pkvm_detach_ptdev()` 实际是切回 host IOMMU 视图，不等于真正 quiesce 设备 DMA。
- `failed to create vm mapping (EINVAL)` 已不再是当前最前置现象。
- 当前最前置现象已经变为 host DMAR fault：
  - `DMA Read NO_PASID`
  - `PTE Read access is not set`
- 这说明当前主线 blocker 已经从 MMIO/config 路径前移到 DMA mirror / IOMMU 映射路径
- 已在本地 `pKVM-IA` 源码落下一版 T2/T3 patch：
  - `pgstate_pgt` 注释与 free 路径已收敛为 DMA mirror 语义，不再在 protected VM 的 shadow teardown 中执行 `__pkvm_host_undonate_guest()`
  - `__pkvm_host_donate_guest()` 后已补一版 runtime DMA mirror 同步，并按 `pgstate_pgt->root_pa` 定向刷 IOTLB
  - `shadow_vm` teardown 顺序已调整为先 detach ptdev，再 deinit `pgstate_pgt`
- 已完成手动增量编译验证：
  - 当前可复用带 `.config` 的构建树完成 host 内核增量编译
  - “这轮机器缺少 `.config`、无法直接复用构建树”不再是当前阻塞
  - 当前结论以手动编译通过后的实机验证为准
- 当前阶段结论：
  - 主线已经从 `BOOT-007/B2/B3` 切到 `BOOT-008/T2/T3`
  - T2/T3 已较高置信度解除 `BOOT-008` 主签名
  - `B4` 已通过本轮修复收敛 `BOOT-009`，因此当前更像是“主线 correctness blocker 已解除，但生命周期和验证补全未完成”，而不是“核心功能仍未打通”

跟踪入口:
- `DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/实施跟踪/00-总览与进展看板.md`
- `DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/实施跟踪/08-GitHub-Issue-PR-协作流.md`
- `DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/实施跟踪/09-run-crosvm-交互式使用方式.md`

下一步:
- 把主线从已关闭的 `BOOT-008/BOOT-009` 正式前移到后续补全项：
  - 单独推进 T4，确认 teardown 前设备 DMA 生命周期正确
  - `T4` 处理完成后，再回到 `pkvm-x86#34`（Host 对 assigned BAR 的 CPU 访问权收口）这条独立缺口
  - 单独推进 T5 / T6，补齐首次 attach、remove-path 和失败回滚
  - 如需把数据路径作为更强硬证据，再补一轮显式 mount 后的 NVMe 写入/回读
  - 若后续回归再暴露新 fault，再按新签名继续拆新 bug，而不是回写到已关闭的 `BOOT-008/BOOT-009`

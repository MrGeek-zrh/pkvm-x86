目标: 将 pVM 设备透传从“源码设计可行”推进到“可分阶段实施、可持续跟踪、可验证收敛”。

当前进度:
- 已确认推荐主线是方案 B: `donate + pVM DMA mirror pgtable`。
- 已确认当前代码里已经存在 `pgstate_pgt -> ptdev->pgt -> IOMMU SLPTR` 这条骨架，但缺少预填充、运行时 mirror、teardown 收敛等关键补全。
- 已建立 `实施跟踪/` 目录，后续实施拆分、依赖关系、状态推进和验证矩阵统一在该目录维护。
- 已完成 T1 修复并通过当前首轮运行验证：旧 shadow spgt 残留 refcount 导致的 donate 阻塞不再复现。
- 已补齐 GitHub Issue / PR 模板，并把 `pKVM-IA` 纳入当前 superproject 的 submodule 管理，后续将按 `Issue -> pKVM-IA PR -> pkvm-x86 PR` 的闭环推进。
- 已在 GitHub 初始化远端管理对象：`pkvm-x86` Project `#3` 和当前 `13` 个 Epic / Task / Bug issues。
- 已新增真正的主线前置任务：`pkvm-x86#13`，用于收敛 protected pVM guest/hyp 的 passthrough MMIO contract。
- 已确认 T1 对应的内核提交已经直接位于 `pKVM-IA` 默认分支，因此无法再补一条对默认分支有实际 diff 的正常 PR；后续内核改动将从 topic branch + PR 流程开始。
- `pkvm-x86#2` 对应的 T1 任务已经完成并通过首轮验证，当前可以关闭；后续阻塞已前移到 `pkvm-x86#5` 和 `pkvm-x86#13`。
- `pkvm-x86#3` 对应的 BOOT-006 bug 也已满足关闭条件：旧 donate/refcount 失败签名已稳定不再复现，当前阻塞同样已前移到 `pkvm-x86#5` 和 `pkvm-x86#13`。

当前卡点:
- `BOOT-007` 的旧签名已经在 2026-03-27 的有效重测中消失，当前最前置问题已切换为 `BOOT-008`：
  - host dmesg 出现 `DMA Read NO_PASID` / `PTE Read access is not set`
  - 这说明 guest CPU 启动链已打通，但设备 DMA 访问仍未收敛正确
- 当前更深的主阻塞已经不再是单纯的 crosvm config/MMIO fallback，而是 `pgstate_pgt -> ptdev->pgt -> IOMMU SLPTR` 这条 DMA mirror 路径仍缺少关键补全。
- 当前 host->pKVM 透传设备接口虽然已经补上 BAR/MMIO metadata contract，但 runtime DMA mirror 仍未跟上；因此主线已经从 B2/B3 前移到 T2/T3。
- 已确认 ARM64 pKVM/AVF 可以作为重要参考对象，但参考重点应放在：
  - device metadata / manifest 通道
  - guest MMIO 分类机制
  - pKVM/hyp 持有 authoritative device state
  - 而不是直接照搬 ARM 的设备 assignment 代码路径
- 已新增 ARM 参考源清单，并约定后续本地参考代码统一落在未跟踪的 `refs/` 目录：
  - `DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/参考实现/ARM-pKVM-参考源.md`
- 已基于本地 ARM 参考树整理出一版设计映射：
  - `DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/参考实现/ARM-pKVM-到-x86-设计映射表.md`
- B3 已收敛出一个更合适的推进方式：先做“上层方案收敛”，再继续细化底层 contract。
- 当前倾向的低复杂度第一阶段目标是：
  - protected pVM
  - 单个 VFIO PCI 设备
  - 静态 attach
  - `NoIommu`
  - config space 继续 emulated
  - 普通 BAR MMIO 作为优先打通的访问面
  - MSI-X table/PBA 继续 emulate 或列为第一阶段非目标
- 在这个上层方案稳定后，B3 再继续细化：
  - 以“受信的 GPA 直达区间表”作为 guest/hyp contract
  - userspace -> host KVM：复用 `KVM_CAP_X86_PROTECTED_VM` 的 `KVM_ENABLE_CAP` flag 空间提交候选区间表
  - authoritative allowlist：必须最终存放在 pKVM/hyp，而不是停留在 host KVM
  - guest -> pKVM/hyp：使用“共享缓冲区 + 分块 hypercall 拷贝”查询直达区间表
  - 注册项至少携带 `guest_gpa + bdf + pasid + bar_index + bar_offset + size + flags`
- B3-1 上层方案已单独沉淀到：
  - `DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/实施跟踪/01C-1-B3-1-protected-pVM-设备透传第一阶段上层方案.md`
- B3-2 已开始细化第一阶段所需的 x86 `ptdev metadata` 结构，当前方向是：
  - `ptdev` 继续承载 identity / IOMMU / attachment state
  - 新增 hyp 内部的富 MMIO metadata
  - guest 只获取精简 allowlist
  - 文档：`DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/实施跟踪/01C-2-B3-2-x86-ptdev-metadata-最小结构草案.md`
  - 当前接口方向：
    - userspace -> host KVM：继续沿用 `KVM_ENABLE_CAP(KVM_CAP_X86_PROTECTED_VM)`
    - guest -> pKVM/hyp：新增查询 allowlist 的 guest hypercall
  - 当前 ABI 决议：
    - metadata 必须在第一次 `KVM_RUN` 前提交
    - 第一阶段冻结 metadata，不支持运行期修改
    - guest 启动早期查询并缓存 allowlist
    - guest allowlist 只暴露 `DIRECT_BAR`
  - 当前已进一步收敛：
    - 推荐 UAPI 常量名与结构体命名
    - 提交接口与查询接口的错误码
    - 幂等提交语义
  - 当前第一轮实现已启动，并已完成：
    - [asm/kvm.h](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/include/uapi/asm/kvm.h) 的 `SET_PTDEV_MMIO_METADATA` UAPI 常量与 userspace 提交结构
    - [kvm_host.h](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/include/asm/kvm_host.h) 的 host 侧 metadata 缓存
    - [pkvm_host.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/vmx/pkvm/pkvm_host.c) 的第一版 copy/validate/cache 处理
    - [pkvm_hypercalls.h](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/include/asm/pkvm_hypercalls.h)、[hyp/vmexit.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/vmexit.c)、[hyp/pkvm.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/pkvm.c) 的 host -> hyp metadata 同步通路
    - [hyp/ptdev.h](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/ptdev.h) 和 [hyp/ptdev.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/ptdev.c) 的 hyp 侧 metadata 绑定
    - [linux/kvm_para.h](/home/mrgeek/pkvm-x86/pKVM-IA/include/uapi/linux/kvm_para.h) 的 guest allowlist 查询 ABI
    - [pkvm/vmx/vmx.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/pkvm/vmx/vmx.c) 的 guest `INFO/READ` hypercall 处理
    - [arch/x86/coco/pkvm/pkvm.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/coco/pkvm/pkvm.c) 的 allowlist 缓存与 `pkvm_virt_mmio()` 分流
    - crosvm 的第一版 metadata 提交通路：
      - [hypervisor/src/x86_64.rs](/home/mrgeek/pkvm-x86/crosvm/hypervisor/src/x86_64.rs) 的 protected pVM ptdev metadata 抽象
      - [hypervisor/src/kvm/x86_64.rs](/home/mrgeek/pkvm-x86/crosvm/hypervisor/src/kvm/x86_64.rs) 的 `KVM_ENABLE_CAP(KVM_CAP_X86_PROTECTED_VM, SET_PTDEV_MMIO_METADATA, ...)` 提交
      - [devices/src/pci/vfio_pci.rs](/home/mrgeek/pkvm-x86/crosvm/devices/src/pci/vfio_pci.rs) 的 VFIO BAR sparse-mmap -> ptdev metadata 导出
      - [arch/src/lib.rs](/home/mrgeek/pkvm-x86/crosvm/arch/src/lib.rs) 的 PCI 设备注册阶段 metadata 提交 hook
      - [src/crosvm/sys/linux/device_helpers.rs](/home/mrgeek/pkvm-x86/crosvm/src/crosvm/sys/linux/device_helpers.rs) 和 [src/crosvm/sys/linux.rs](/home/mrgeek/pkvm-x86/crosvm/src/crosvm/sys/linux.rs) 的 protected VM 调用路径
  - 已完成 crosvm userspace 本地构建验证：
    - 已安装 Rust `1.77.2` 工具链并完成 `cargo build -p crosvm --locked`
    - 构建过程中已修复的本地编译问题包括：
      - [hypervisor/src/kvm/x86_64.rs](/home/mrgeek/pkvm-x86/crosvm/hypervisor/src/kvm/x86_64.rs) 的常量名误写
      - [arch/src/lib.rs](/home/mrgeek/pkvm-x86/crosvm/arch/src/lib.rs) 的 `DeviceRegistrationError` 枚举排序约束
      - [x86_64/src/lib.rs](/home/mrgeek/pkvm-x86/crosvm/x86_64/src/lib.rs) 对 `components.hv_cfg.protection_type` 的字段访问修正
  - 已完成第二轮 crosvm userspace 路径收敛并通过本地构建：
    - [hypervisor/src/kvm/x86_64.rs](/home/mrgeek/pkvm-x86/crosvm/hypervisor/src/kvm/x86_64.rs) 的 `KvmVm::is_pkvm()` 已改为复用 `get_protected_vm_info()`
    - [x86_64/src/lib.rs](/home/mrgeek/pkvm-x86/crosvm/x86_64/src/lib.rs) 已在 protected VM 下停止：
      - 创建 `PciVirtualConfigMmio`
      - 向 `generate_pci_root()` 传入 `vcfg_base`
      - 在 ACPI 中公开 `VCFG`
    - 已本地复用绝对路径验证通过：`/home/mrgeek/.cargo/bin/cargo build -p crosvm --locked`
  - 已完成 2026-03-27 的有效端到端运行验证：
    - guest 已启动到 Ubuntu login prompt
    - `Failed to map mmio page; failed to create vm mapping` 未再出现
    - `vcpu hit unknown error: Bad address (os error 14)` 未再出现
    - host dmesg 新暴露出的独立签名为：
      - `DMAR: [DMA Read NO_PASID] Request device [01:00.0] fault addr ... [fault reason 0x06] PTE Read access is not set`
    - 当前新问题已单独记录为：
      - [BOOT-008-protected-pVM-NoIommu-VFIO-host-DMAR-PTE-Read-access-not-set.md](/home/mrgeek/pkvm-x86/DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/问题记录/BOOT-008/BOOT-008-protected-pVM-NoIommu-VFIO-host-DMAR-PTE-Read-access-not-set.md)
  - 已完成第一次端到端运行验证，但 `BOOT-007` 仍然复现：
    - `Loaded bzImage kernel` 之后仍触发 `vcpu hit unknown error: Bad address (os error 14)`
    - 当前 `Failed to map mmio page` 已从多次下降到 1 次，说明第一轮改动改变了前期 config/MMIO 症状，但没有解除最终 blocker
    - 当前更可疑的残余路径已收敛到 protected VM 下仍然存在的 `PciVirtualConfigMmio` / ACPI `VCFG` / VFIO virtual-config MMIO 访问链
  - 已修复当前 host 内核编译 blocker：
    - [pkvm/vmx/vmx.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/pkvm/vmx/vmx.c) 的 `__PKVM_HYP__` guest allowlist 回写路径误用了普通 KVM 的 `kvm_vcpu_write_guest()`
    - `vmlinux` 链接阶段因此出现 `undefined reference to kvm_vcpu_write_guest__pkvm`
    - 现已改为使用 hyp 侧 [memory.c](/home/mrgeek/pkvm-x86/pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/memory.c) 的 `write_gpa()`
    - 已本地增量验证通过：`make -C pKVM-IA -j1 vmlinux`
- B2 当前已完成其 workaround 目标：旧失败点已明显后移；它不替代后续 DMA mirror 主线。
- 当前已经需要把新签名单独拆出：
  - `BOOT-007` 的旧签名已不再复现
  - 新的 host DMAR fault 已单独记录为 `BOOT-008`
- `pgstate_pgt` 当前还带有“pVM teardown 时 undonate 页面”的旧语义，需要收敛成纯 DMA mirror 语义。
- VM 销毁阶段当前是“先销毁页表，再处理 ptdev”，且现有 `pkvm_detach_ptdev()` 实际是切回 host IOMMU 视图，不等于真正 quiesce 设备 DMA。
- `failed to create vm mapping (EINVAL)` 已不再是当前最前置现象。
- 当前最前置现象已经变为 host DMAR fault：
  - `DMA Read NO_PASID`
  - `PTE Read access is not set`
- 这说明当前主线 blocker 已经从 MMIO/config 路径前移到 DMA mirror / IOMMU 映射路径

跟踪入口:
- `DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/实施跟踪/00-总览与进展看板.md`
- `DOCS/需求/分析当前pKVM-IA对设备透传的支持情况/实施跟踪/08-GitHub-Issue-PR-协作流.md`

下一步:
- 继续把当前状态从 `BOOT-007/B2` 正式前移到 `BOOT-008/T2/T3`：
  - `BOOT-007` 作为旧签名保留历史
  - `BOOT-008` 作为新签名单独跟踪
- 进入 T2/T3 代码分析与修复准备：
  - `pgstate_pgt` 语义收敛为纯 DMA mirror
  - donate 后同步 runtime DMA mirror
  - 按新的 `root_pa` 定向刷 IOTLB

## Codex Rules
- GitHub 管理应遵循“状态、证据、实现分层”的理念：
  - GitHub Issue/Project 负责表达当前真实状态和推进关系。
  - 本地 `DOCS/...` 负责承载长文证据、原始日志、源码分析、复现记录和完整方案设计。
  - 代码仓库中的 PR/commit 负责承载具体实现，不代替 issue 做问题管理。
- GitHub 管理应遵循“问题、动作、集成分离”的理念：
  - `Bug` 负责记录问题签名和问题分析。
  - `Task` 负责记录修复动作和阶段目标。
  - `PR` 负责提交单一目标的实现或集成，不承担问题归档职责。
- GitHub 管理应遵循“先保留症状，再推进修复”的理念：
  - 先把现象、关键日志、原始分析文档和原始报错文件固定到 issue。
  - 再推进 Task、PR 和验证；不要让代码已经推进，但问题证据仍然散落在聊天记录里。
- GitHub 管理应遵循“旧问题关闭、新问题新建”的理念：
  - 已解决的旧签名应关闭并保留为历史 blocker。
  - 修复旧签名后暴露的新签名必须新建 issue，不能覆盖旧 issue 的历史语义。
- 当需要分析代码实现和原理时，必须结合源码进行讲解，并引用相关文件路径。
- 除非用户明确要求，否则分析内核代码时默认只分析 `/home/mrgeek/pkvm-x86/pKVM-IA` 下的代码。
- 代码调用关系的临时展示使用 4 空格缩进来表示不同函数间的调用层级。
- 若调用栈/调用链需要发布到 GitHub issue / PR / comment 中，必须放在 fenced code block 里（例如 ```text```），不要依赖普通段落中的前导空格缩进；否则 GitHub 会折叠缩进，导致层级难以阅读。
- 对复杂任务进行分阶段推进时，必须同步维护对应的任务管理文件；当任务状态、阶段结论、当前阻塞或下一步发生实质变化后，应及时更新跟踪文档，而不是等到任务结束后一次性补写。
- `pkvm-x86` 默认作为总控仓库使用：GitHub Issue、Project、文档、复现脚本、自动化和任务状态以该仓库为主。
- `pKVM-IA` 默认作为内核实现仓库使用：内核代码改动应在 `pKVM-IA` 中提交并通过独立 PR 管理。
- commit message 默认采用“两行写法”：
  - 第一行只写单一目标的简短概括。
  - 第二行补一行展开说明，交代这次提交主要同步了什么、为什么要做这一步。
  - 若需要更多细节，从第三行起继续补充；不要默认只写单行标题式 commit message。
- 复杂问题默认采用 `Issue -> 修复 PR -> superproject PR` 的闭环；其中 superproject PR 负责同步文档、脚本及必要的 submodule 指针。
- 不要求每次都同时提交 `pKVM-IA PR` 和 `pkvm-x86 PR`：
  - 若只改 `pKVM-IA` 内核代码，默认先提交 `pKVM-IA PR`。
  - 若只改 `pkvm-x86` 文档、脚本、自动化，只提交 `pkvm-x86 PR`。
  - 若 `pKVM-IA` 改动已经通过一轮验证，或需要把 superproject 固定到某个已验证的内核 commit，再提交 `pkvm-x86 PR` 更新 submodule 指针和相关文档。
  - `pkvm-x86 PR` 默认作为“集成快照 PR”使用，而不是要求每个内核提交都立即跟一个 superproject PR。
- 一个 panic/报错签名对应一个独立 bug issue；如果修复旧签名后暴露出新签名，必须新建 issue，不能继续混在旧 issue 中追踪。
- 同一技术问题如果同时包含“具体报错签名”和“实现修复动作”，应拆成 `Bug + Task` 两个 issue，而不是合并成一个：
  - `Bug` issue 负责记录唯一的 panic/报错签名、现象、日志和关闭条件。
  - `Task` issue 负责记录修复动作、阶段目标、实现范围和验收标准。
  - 不要让 `Task` 代替 `Bug` 记录签名，也不要让 `Bug` 代替 `Task` 承载实现计划。
  - 若某个 `Task` 主要承载的是“语义设计 / contract 设计 / 阶段进展记录”，它不能替代某个具体 `Bug` 的修复动作 issue。
  - 设计类 `Task` 可以作为前置依赖或阶段记录，但当 `Bug` 进入实际修复阶段时，仍应有一个面向该 `Bug` 的修复 `Task`，专门描述修哪条路径、怎么验收。
- `Bug` issue 和对应 `Task` issue 必须显式双向关联：
  - `Task` 正文中写明 `关联 Bug`。
  - `Bug` 正文中写明 `关联 Task`。
  - 关闭 comment 中也应说明“由哪个 Task 修复”或“当前阻塞已前移到哪个 Bug/Task”。
- `Bug` 一旦进入“准备修复 / 正在修复”状态，必须有对应的 `Task` issue 承载修复方案；不要出现“代码已经开始改，但 GitHub 上还没有修复方案”的状态：
  - 若只是保留现象、日志和归因，`Bug` issue 可以暂时没有修复方案。
  - 一旦决定开始修，必须新建或补齐对应 `Task` issue。
  - `Task` issue 至少写清：`当前表现 / 当前阻塞`、`修复方案摘要`、`实现范围`、`非目标`、`验收标准`，并给出本地方案设计文件入口（若已有详细方案）。
  - GitHub `Task` issue 负责承载可执行摘要和推进状态；完整方案设计仍应落在本地 `DOCS/...`。
  - 不允许只在聊天、PR review 或 commit message 中口头描述修复方案，而不在 GitHub issue 和本地方案文档中落盘。
- `Bug` 与 `Task` 的关闭时机分别判断，不要求同时关闭：
  - `Bug` 在对应签名经验证不再复现后关闭。
  - `Task` 在其预期修复动作或阶段目标完成后关闭。
  - 同一轮里两者都满足条件时，可以一起关闭；否则按各自状态单独处理。
- 一个 PR 只解决一个明确目标；不要在同一个 PR 中同时修复两个不同签名的问题。
- GitHub Issue/Project 作为任务状态真相来源；本地 `DOCS/.../实施跟踪` 和 `DOCS/.../问题记录` 主要保存长文分析、原始日志、源码证据和复现命令。
- 远端 GitHub issue 的正文开头必须先保留“现象/当前表现”摘要：
  - `Bug` issue 开头先写 panic/报错签名、当前现象和最小影响。
  - `Task` issue 开头先写当前阻塞/当前表现，再写目标、修复方案摘要、依赖以及本地方案文件入口。
  - 不要让最关键的现象只出现在长评论或本地文档里。
- `Task` issue 正文必须显式有“修复方案摘要”段，不能只写目标不写路径：
  - 至少回答“准备改哪里、为什么这样改、这轮明确不做什么”。
  - 若本地已经有详细设计，正文中应链接对应 `DOCS/...` 文件，而不是把长方案全文塞进 GitHub issue。
  - 若方案还没完全定稿，也要写当前候选方案和选择条件。
  - 进入 `status/in-progress` 前，`Task` issue 中应已存在可执行的修复方案摘要。
- `Bug` issue 的 `现象` 段应优先给出“完整的关键日志信息”，再给摘要解释：
  - 至少贴出 3-10 行最能代表该签名的原始日志。
  - 日志应尽量保持原样，避免只写口头概括。
  - 若有更完整日志，正文保留关键节选，但必须在仓库中保存完整原始日志文件，并从 issue 或问题文档中给出可点击引用；不要只留在聊天记录里。
  - 不要把关键日志只放在 comment 里，正文里必须能直接看出签名。
- `Bug` issue 正文应尽量补一个“原始报错信息文件 / 原始分析文档”引用：
  - 若原始日志文件或分析文档已纳入仓库，优先使用可点击的 GitHub 文件链接。
  - 若日志文件过大，不直接贴全文，在 issue 中放关键节选，并附上对应文件链接。
  - 若日志文件和分析文档同时存在，优先两者都给。
  - 若文件仅本地存在而未纳入仓库，应至少写明本地路径和来源，避免证据失联。
- 在 GitHub issue / PR / comment 中引用 issue、PR、commit 时，不要用反引号包裹编号，否则 GitHub 不会自动生成超链接；跨仓库引用必须写成 `owner/repo#num`，或直接使用完整 URL。
- 当某个 `Task` 或 `Bug` 已经开始实际推进时，GitHub Project 中对应卡片状态也必须同步更新：
  - 当前正在做的主任务应至少标为 `In Progress`，不要长期停在 `Todo`。
  - 若当前主线 blocker 已切换，应同步更新 issue label / project status，而不是只在本地文档里改。
- 若 `pKVM-IA` 被纳入当前仓库的 submodule，则 `pkvm-x86` 中对应 PR 必须同步更新 submodule 指针，并记录所关联的 `pKVM-IA` PR/commit。
- 记录问题时，不在单个总文件持续追加；每个新问题都在 `DOCS/问题清单/` 下新建一个独立 Markdown 文件，按统一模版组织：
  - 文件名建议：`[分类-编号]-问题名.md`（如 `BOOT-001-xxx.md`、`IMG-001-xxx.md`）
  - 标题：`# [分类-编号] 问题名`
  - 必填字段：`现象`、`根因（简述）`、`解决方案`、`验证要点`
  - 证据字段（至少一项）：`原始日志（节选）` 或 `触发条件/复现场景` 或 `触发路径（常见回溯）`
  - 若已有完整原始报错输出，必须在同目录保存完整日志文件，正文用节选并附文件引用；不要只保留节选。
  - 可选字段：`影响`、`环境信息（来自日志）`、`线索`、`备注`
  - 若涉及源码或配置开关，需写明具体文件路径、配置项或命令（必要时给最小复现命令）
- 若某个问题明确属于某个专题/需求目录，应优先记录在该专题目录下的 `问题记录/` 子目录，而不是放到通用 `DOCS/问题清单/`。
下面是一个示例：
pKVM (hypervisor)
  __pkvm_host_donate_guest(...)                    (pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/mem_protect.c)
    do_donate(tx)
      host_initiate_donation(tx)                  (pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/mem_protect.c)
        host_ept_set_owner_locked(..., owner_id)  (pKVM-IA/arch/x86/kvm/vmx/pkvm/hyp/mem_protect.c)
          pkvm_pgtable_annotate(host_ept, addr, size, annotation)
            (unmap from host EPT, keep owner info in invalid PTE)
